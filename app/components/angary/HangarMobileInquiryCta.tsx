import { AttachedBriefCta } from '../inquiry/AttachedBriefCta';

/** /angary's phone shortcut to the form, once the configurator summary has scrolled past. */
export function HangarMobileInquiryCta() {
  return <AttachedBriefCta gate=".hc-summary-flagship" className="angary-mobile-inquiry-cta" />;
}
