/**
 * Version stamp for everything the Grain Planner hands over — the brief text and the structured
 * state attached to an inquiry. Bump it whenever questions, answer ids or decision rules change,
 * so a stored lead can always be read against the logic that produced it.
 *
 * 1.0.0 = the behaviour of prototype codex/grain-planner-v0.5 @ 819f163 (golden-pinned).
 */
export const GRAIN_PLANNER_VERSION = 'grain-planner@1.0.0';
